<Window
    x:Class="lab4_18.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MainPage"
    Width="820"
    Height="720"
    WindowStartupLocation="CenterScreen" >

    <Window.Resources>
        <ResourceDictionary Source="../styles.xaml" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Header -->
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>      <!-- List -->
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>   <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- ===== Header: navigation/actions ===== -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
            <Button Content="Пошук"
                    Click="Search"
                    Height="42"
                    Width="120"
                    Margin="0,0,12,0"
                    DockPanel.Dock="Left"/>

            <Button Content="Оновити дані про себе"
                    Click="UpdateInformationClick"
                    Height="42"
                    Width="220"
                    Margin="0,0,12,0"
                    DockPanel.Dock="Left"/>

            <Button Content="Вийти"
                    Click="Close"
                    Height="42"
                    Width="120"
                    DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- ===== Content card: students list ===== -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource SurfaceStrokeBrush}"
                BorderThickness="1"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- caption -->
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>   <!-- list -->
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Список студентів"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Students}"
                         Name="StudentsList"
                         BorderThickness="0"
                         Background="{StaticResource SurfaceBrush}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource SurfaceStrokeBrush}"
                                    BorderThickness="0 0 0 1"
                                    Padding="8,4">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Left"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Name}"
                                                   Width="180"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding Rating}"
                                                   Width="100"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </StackPanel>

                                    <Button Content="Переглянути детальну інформацію"
                                            Click="DetailedInfoClick"
                                            Tag="{Binding}"
                                            Height="30"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- ===== Footer button ===== -->
        <StackPanel Grid.Row="4" HorizontalAlignment="Right">
            <Button Content="Переглянути дедлайни"
                    Click="DeadlineClick"
                    Height="42"
                    Width="260"/>
        </StackPanel>
    </Grid>
</Window>
