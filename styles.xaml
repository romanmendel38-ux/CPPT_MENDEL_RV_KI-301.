<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Palette (reusable brushes) ===== -->
    <SolidColorBrush x:Key="PrimaryBrush"          Color="#6750A4"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush"     Color="#7C67C3"/>
    <SolidColorBrush x:Key="PrimaryPressedBrush"   Color="#59408F"/>
    <SolidColorBrush x:Key="OnPrimaryBrush"        Color="#FFFFFF"/>

    <SolidColorBrush x:Key="SurfaceBrush"          Color="#FFFBFE"/>
    <SolidColorBrush x:Key="SurfaceAltBrush"       Color="#F6F2FA"/>
    <SolidColorBrush x:Key="SurfaceStrokeBrush"    Color="#E5E1EC"/>

    <SolidColorBrush x:Key="TextPrimaryBrush"      Color="#1D192B"/>
    <SolidColorBrush x:Key="TextSecondaryBrush"    Color="#3C3750"/>
    <SolidColorBrush x:Key="DangerBrush"           Color="#D32F2F"/>

    <!-- High Contrast fallbacks (DynamicResource picks system brushes when HC on) -->
    <SolidColorBrush x:Key="FocusBrush" Color="#6750A4"/>

    <!-- ===== Default Button style (modern) ===== -->
    <Style TargetType="Button">
        <Setter Property="Background"       Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground"       Value="{StaticResource OnPrimaryBrush}"/>
        <Setter Property="BorderBrush"      Value="{StaticResource SurfaceStrokeBrush}"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Padding"          Value="12,8"/>
        <Setter Property="FontSize"         Value="14"/>
        <Setter Property="FontWeight"       Value="SemiBold"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" >
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"/>
                        </Border>

                        <!-- Keyboard focus ring -->
                        <Border x:Name="FocusVisual"
                                IsHitTestVisible="False" 
                                BorderThickness="2"
                                BorderBrush="{StaticResource FocusBrush}"
                                Opacity="0"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                        </Trigger>

                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                            <Setter Property="Cursor"  Value="Arrow"/>
                        </Trigger>

                        <!-- Keyboard focus ring (visible when focused) -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- Default button (Enter key) accent -->
                        <Trigger Property="IsDefaulted" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Border (cards / containers) ===== -->
    <Style TargetType="Border">
        <Setter Property="Background"      Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource SurfaceStrokeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <!-- ===== Text styles (keys unchanged) ===== -->

    <!-- Стиль для лейблів -->
    <Style TargetType="TextBlock" x:Key="LabelStyle">
        <Setter Property="FontWeight"        Value="SemiBold"/>
        <Setter Property="Margin"            Value="0,0,8,8"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground"        Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="TextWrapping"      Value="Wrap"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ValueStyle">
        <Setter Property="Margin"       Value="0,0,0,8"/>
        <Setter Property="Padding"      Value="4,2"/>
        <Setter Property="Background"    Value="Transparent"/>
        <Setter Property="Foreground"    Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="TextWrapping"  Value="Wrap"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ImportantTextStyle">
        <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

</ResourceDictionary>
