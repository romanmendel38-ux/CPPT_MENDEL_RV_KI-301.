<Window
    x:Class="lab4_18.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:lab4_18"
    mc:Ignorable="d"
    Title="Вхід"
    Width="520"
    Height="480"
    WindowStartupLocation="CenterScreen" >

    <Window.Resources>
        <ResourceDictionary Source="styles.xaml" />
    </Window.Resources>

    <!-- Центрована «картка» з формою -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Column="1"
                Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource SurfaceStrokeBrush}"
                BorderThickness="1"
                Padding="28"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>   <!-- Header -->
                    <RowDefinition Height="20"/>    <!-- Spacer -->
                    <RowDefinition Height="Auto"/>   <!-- Form -->
                    <RowDefinition Height="20"/>    <!-- Spacer -->
                    <RowDefinition Height="Auto"/>   <!-- Actions -->
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <StackPanel Grid.Row="0" Margin="0,0,0,0">
                    <TextBlock Text="Вітаємо 👋"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Увійдіть до облікового запису"
                               Margin="0,6,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>

                <!-- Форма -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Username -->
                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Style="{StaticResource LabelStyle}"
                               Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               Text="Username"/>
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Width="220"
                             Height="32"
                             Padding="8,4"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             VerticalAlignment="Center"
                             TabIndex="0"/>

                    <!-- Password -->
                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Style="{StaticResource LabelStyle}"
                               Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               Text="Password"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Grid.Row="2"
                                 Grid.Column="1"
                                 Width="220"
                                 Height="32"
                                 Padding="8,4"
                                 VerticalAlignment="Center"
                                 TabIndex="1"/>
                </Grid>

                <!-- Кнопки дій -->
                <Grid Grid.Row="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Height="36"
                            Content="Register"
                            Click="RegisterClick"
                            TabIndex="2"/>

                    <Button Grid.Column="2"
                            Height="36"
                            Content="Login"
                            Click="ButtonClick"
                            IsDefault="True"
                            TabIndex="3"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
